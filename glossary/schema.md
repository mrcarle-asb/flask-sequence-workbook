---
name: Schema
---

The structure definition of a database table — its column names, data types, and constraints. A schema like `CREATE TABLE birthdays (id INTEGER PRIMARY KEY, name TEXT NOT NULL, month INTEGER)` tells the database what shape the data must have. The database enforces the schema on every insert and update.
