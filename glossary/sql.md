---
name: SQL
---

Structured Query Language — a standard language for reading and writing data in a database. Instead of writing code to open files and parse rows, you write queries like `SELECT * FROM birthdays` and the database handles the rest. SQL appears in IB Computer Science Paper 2 and is used in nearly every backend web application.
