---
name: DRY
---

Don't Repeat Yourself. A principle that says if the same code or structure appears in multiple places, it should live in one place and be referenced from the others. In Flask, `base.html` is DRY applied to HTML — shared page structure defined once, inherited by every page.
