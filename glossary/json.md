---
name: JSON
---

JavaScript Object Notation — a text format for structured data, used by most web APIs. It looks like Python dictionaries and lists: `{"name": "Alice", "age": 30}`. In Python, `response.json()` parses a JSON response into native dicts and lists. The `json` module in the standard library can also read and write JSON files.
