---
name: API
---

Application Programming Interface — in web development, usually a URL that returns structured data (JSON) instead of a web page. Your code calls the API, parses the response, and uses the data. Examples: weather APIs, transit APIs, payment APIs. Flask apps often act as both an API server (serving data to browsers) and an API client (fetching data from other services).
